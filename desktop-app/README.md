# Desktop App (Electron + React)

This is a placeholder folder for your Electron + React app.
You can initialize it with:

npx create-electron-app .
or
npm create vite@latest my-app --template react-ts
